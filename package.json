{
  "name": "admaxxing-landing",
  "version": "1.0.0",
  "private": true,
  "description": "Admaxxing landing site + signup API (Vercel static + serverless functions)",
  "scripts": {
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  }
}
